version: '3.8'

services:
  chatbot:
    build: .
    container_name: test_chatbot
    volumes:
      - ./data:/app/data
      - ./vectordb:/app/vectordb
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    network_mode: host

